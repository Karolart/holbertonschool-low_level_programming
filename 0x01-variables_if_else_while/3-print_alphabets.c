#include <stdio.h>
/**
 * main - Entry point, prints the caracters of the alphabet in lowercase and upercase
(*
 * Return: 0 on success
 */

int main(void)
{
        char c;


        for (c = 'a'; c <= 'z'; c++)
        {
                putchar(c);
        }

        for (c = 'A'; c <= 'Z'; c++)


        for (ch = 'A'; c <= 'Z'; c++)
        {
                putchar(c);
        }

        putchar('\n');

        return (0);
}


