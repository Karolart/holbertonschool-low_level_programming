#include <stdio.h>

/**
  * main - Prints a setence 
  *
  * Return: 0 (always if is sucess)
  */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}

